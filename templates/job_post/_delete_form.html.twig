<form method="post" action="{{ path('app_job_post_delete', {'id': job_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_post.id) }}">
    <button class="btn">Delete</button>
</form>
