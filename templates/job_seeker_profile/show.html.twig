{% extends 'base.html.twig' %}

{% block title %}JobSeekerProfile{% endblock %}

{% block body %}
    <h1>JobSeekerProfile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_seeker_profile.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ job_seeker_profile.fullName }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ job_seeker_profile.resume }}</td>
            </tr>
            <tr>
                <th>JobType</th>
                <td>{{ job_seeker_profile.jobType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_seeker_profile_index') }}">back to list</a>

    <a href="{{ path('app_job_seeker_profile_edit', {'id': job_seeker_profile.id}) }}">edit</a>

    {{ include('job_seeker_profile/_delete_form.html.twig') }}
{% endblock %}
